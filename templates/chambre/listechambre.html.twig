{% extends 'base.html.twig' %}

{% block title %}Liste chambre{% endblock %}

{% block body %}
    <div class="babs prend ca">
        <div class="row border-bottom mb-4">
            <div class="col-6 d-flex justify-content-start align-items-center">
                <h4 class="color-general mt-5 mb-5 font-weight-normal">Lister, modifier ou supprimer<span class="font-weight-light"> chambres</span></h4>
            </div>
            <div class="col-6 d-flex justify-content-end align-items-center">
                <img src="{{ asset('/source/image/icones/bed.png') }}" class="icone-menu" alt="">
            </div>
        </div>
        <div class="row justify-content-center align-item-center p-2">
            <div class="col-xl-3">

            </div>

        </div>
        <div class="row justify-content-center align-item-center">
            <div class="w-100">
                <table class="table table-striped table-bordered text-center table-hover display" id="table-list-chambre">
                    <thead class="thead-dark">
                    <tr>
                        <th scope="col">Numero chambre</th>
                        <th scope="col">Type chambre</th>
                        <th scope="col">Numero Batiment</th>
                        <th scope="col">Modifier</th>
                        <th scope="col">Supprimer</th>
                    </tr>
                    </thead>
                </table>
            </div>
        </div>
    </div>


{% endblock %}
{% block javascripts %}
    {{ parent() }}
    {#    {{ encore_entry_script_tags('listeEtudiant') }}#}
    {{ encore_entry_script_tags('tableChambre') }}
    <script src="{{ asset('bundles/fosjsrouting/js/router.min.js') }}"></script>
    <script src="{{ path('fos_js_routing_js', { callback: 'fos.Router.setData' }) }}"></script>
{% endblock %}
